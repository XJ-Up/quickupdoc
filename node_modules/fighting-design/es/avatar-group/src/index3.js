import { setStringNumberProp as t, setBooleanProp as e, setStringProp as o } from "../../_utils/props/index.js";
import { isString as i, isNumber as n } from "../../_utils/is/index.js";
import { FIGHTING_SIZE as s, FIGHTING_FIT as p } from "../../_tokens/attrs/index.js";
const f = {
  /** 偏移距离 */
  positionSize: t(),
  /** 是否为圆角 */
  round: e(!0),
  /**
   * 头像大小
   *
   * 可传入字符串使用内置大小
   *
   * 也可以传入数字，数字将自动转换为 px 单位
   *
   * @values large middle small mini
   * @default middle
   */
  size: {
    type: [String, Number],
    default: () => "middle",
    validator: (r) => i(r) ? s.includes(r) : n(r) ? r >= 1 : !1
  },
  /** 是否懒加载头像 */
  lazy: e(),
  /**
   * 如何适应容器
   *
   * 原生样式属性
   *
   * @see object-fit https://developer.mozilla.org/en-US/docs/Web/CSS/object-fit#try_it
   * @values fill contain cover none scale-down
   */
  fit: o(void 0, (r) => p.includes(r))
}, S = Symbol(
  "avatar-group-props-key"
);
export {
  S as AVATAR_GROUP_PROPS_KEY,
  f as Props
};
