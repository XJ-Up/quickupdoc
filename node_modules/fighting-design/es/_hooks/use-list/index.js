import { computed as u, ref as $, reactive as h } from "vue";
import { isBoolean as f, isNumber as a, isString as g, isArray as _ } from "../../_utils/is/index.js";
import { convertFormat as c, sizeChange as m } from "../../_utils/utils/index.js";
import { useProps as b } from "../use-props/index.js";
const B = (p, n) => {
  const { filter: i } = b(p), L = (r, e) => u(() => {
    const t = $([]), s = i(r);
    e && t.value.push(e);
    for (const o in s)
      s[o] && t.value.push(
        `f-${n}__${f(s[o]) ? c(o) : s[o]}`
      );
    return t.value;
  }), y = (r, e, t = !0) => {
    if (f(t))
      return a(r) && t ? m(r) : r;
    if (g(t)) {
      if (t === e)
        return r;
    } else if (_(t)) {
      for (const s of t)
        if (s === e)
          return r;
    }
    return m(r);
  };
  return { classes: L, styles: (r, e = !0) => u(() => {
    const t = h({}), s = i(r);
    for (const o in s)
      s[o] && (t[`--${n}-${c(o)}`] = y(
        s[o],
        o,
        e
      ));
    return t;
  }) };
};
export {
  B as useList
};
