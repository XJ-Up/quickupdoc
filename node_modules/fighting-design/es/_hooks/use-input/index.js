import { useRun as u } from "../use-run/index.js";
const d = (n, e) => {
  const { run: t } = u();
  return {
    handleInput: (a) => {
      t(n.onInput, e.value, a);
    },
    handleChange: (a) => {
      t(n.onChange, e.value, a);
    },
    handleClear: () => {
      n.disabled || (e.value = "");
    }
  };
};
export {
  d as useInput
};
