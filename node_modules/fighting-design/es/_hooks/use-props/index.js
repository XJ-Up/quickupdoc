import { reactive as s, toRef as o } from "vue";
import { isString as l, isObject as n } from "../../_utils/is/index.js";
const k = (t) => ({
  filter: (e) => {
    const c = s({});
    return e.forEach((r) => {
      l(r) ? c[r] = o(t, r) : n(r) && r.callback && r.callback() && (c[r.actualKey || r.key] = o(t, r.key));
    }), c;
  },
  interceptors: (e, c, r = null) => c() ? o(t, e) : r
});
export {
  k as useProps
};
