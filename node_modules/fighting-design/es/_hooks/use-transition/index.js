import { useRun as d } from "../use-run/index.js";
const { run: o } = d(), c = (e) => ({
  handleOpen: (n) => {
    document.body.style.overflow = "hidden", o(e.onOpen, n);
  },
  handleOpenEnd: (n) => {
    o(e.onOpenEnd, n);
  },
  handleClose: (n) => {
    document.body.style.overflow = "", o(e.onClose, n);
  },
  handleCloseEnd: (n) => {
    o(e.onCloseEnd, n);
  }
});
export {
  c as useTransition
};
