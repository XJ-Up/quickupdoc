import { isArray as o, isObject as i } from "../is/index.js";
const p = (e, n) => {
  let r = [];
  return o(e) && e.length && e.forEach((t) => {
    const s = i(t.type) && t.type.name;
    if (s === n && r.push(t), s !== n && t.children && o(t.children)) {
      const f = p(
        t.children,
        n
      );
      r = r.concat(f);
    }
  }), r;
};
export {
  p as getChildren
};
