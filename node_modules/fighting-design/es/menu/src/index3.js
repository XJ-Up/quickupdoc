import { setStringProp as n, setStringNumberProp as r, setFunctionProp as o } from "../../_utils/props/index.js";
const i = {
  /**
   * 导航栏模式
   *
   * 水平的 垂直的 内联的
   *
   * @values horizontal vertical inline
   * @default horizontal
   */
  mode: n("horizontal", (e) => ["horizontal", "vertical", "inline"].includes(e)),
  /** 默认激活的 name */
  activeName: r(),
  /** menu-item 点击执行的回调 */
  onMenuItemClick: o(),
  /** submenu 点击执行的回调 */
  onSubmenuClick: o()
}, m = Symbol("menu-provide-key");
export {
  m as MENU_PROVIDE_KEY,
  i as Props
};
